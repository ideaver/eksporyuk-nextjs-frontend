query ArticleFindOne($where: ArticleWhereUniqueInput!) {
  articleFindOne(where: $where) {
    id
    title
    content
    isActive
    createdByAdminId
    createdAt
    updatedAt
    target
    category {
      id
      name
    }
    fileUrl {
      path
    }
  }
}
