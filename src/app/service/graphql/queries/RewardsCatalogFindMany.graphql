query RewardsCatalogFindMany($take: Int, $skip: Int, $where: RewardsCatalogWhereInput) {
  rewardsCatalogFindMany(take: $take, skip: $skip, where: $where) {
    id
    title
    course {
      basePrice
      status
    }
    pointsRequired
    createdAt
  }
}